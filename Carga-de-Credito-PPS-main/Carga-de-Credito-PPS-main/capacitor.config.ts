import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'carga.credito',
  appName: 'CargaDeCreditoMaximilianoLenzuen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
